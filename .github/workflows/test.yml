name: TEST CI

on:
  workflow_dispatch:

jobs:
  test_ci:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Add reviewers
        uses: actions/github-script@v4
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
          script: |
            const repo = context.repo
            console.log(repo)
            const owner = context.repo.owner
            console.log(owner)
            // owner is the organization name
            // now get the access info of the repo
            const access = await github.repos.get({
              owner: owner,
              repo: repo
            })
